import { ImageByMainPipe } from './image-by-main.pipe';

describe('ImageByMainPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageByMainPipe();
    expect(pipe).toBeTruthy();
  });
});
